import React from 'react';
import ReactDOM from 'react-dom/client';
import { SchemaStudioApp } from './schema-studio.jsx';
import './schema-studio.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SchemaStudioApp />
  </React.StrictMode>
);
